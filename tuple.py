import os
import sys
import random

pi_tuple = (3,0,4,1,5,9)
new_tuple = list(pi_tuple) #make the tuple a list
new_list = tuple(new_tuple) # make the list a tuple

print(len(pi_tuple))
print(min(pi_tuple))
print(max(pi_tuple))